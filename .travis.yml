language: cpp
compiler:
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libdcmtk2-dev libboost-test-dev
script: mkdir build && cd build && cmake -D CMAKE_CXX_FLAGS="-std=c++0x" ../ && make && ../tests/run.sh
