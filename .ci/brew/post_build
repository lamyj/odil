#!/bin/sh

set -ev

export Python=${Python:?}
export WORKSPACE=${WORKSPACE:?}

cd build

export PYTHONPATH=${WORKSPACE}/install
../tests/run --no-network --nose ~/Library/Python/${Python}*/bin/nosetests
